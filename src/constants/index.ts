export enum STATUS {
  Idle = 'idle',
  Loading = 'loading',
  Succeeded = 'succeeded',
  Failed = 'failed',
}

export enum TRANSFER_OPTIONS {
  All = -1,
  Null = 0,
  One = 1,
  Two = 2,
  Three = 3,
}

export enum SORT_OPTIONS {
  Price = 0,
  Duration = 1,
  Optimal = 2,
}
